import 'package:flutter/cupertino.dart';

class GamePlay extends StatelessWidget{
  const GamePlay({Key ?Key});
  GameWidget(gamableSprite) {return Key;}

  @override
  Widget build(BuildContext context) {
    return GameWidget(GamePlay());
  }

  //


}